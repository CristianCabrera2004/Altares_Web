services:
  - type: web
    name: libreria-altares-frontend
    env: static
    rootDir: libreria-frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist/libreria-frontend/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
